# Proteção do diretório de armazenamento (storage) do LPAF
# Impede o download direto de CSVs, logs, backups e dados temporários.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
